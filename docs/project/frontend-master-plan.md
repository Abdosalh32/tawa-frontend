# خطة الواجهة الأمامية — منصة توا (النسخة المصحَّحة)

> **الغرض:** مرجع واحد لحالة المشروع الفعلية وقواعد العمل والتسلسل القادم.
> **مصدر الحقيقة للمنتج:** `docs/requirements/توا-المتطلبات.pdf` ثم `docs/ux/` و`docs/design/`.
> **آخر تحديث:** 17 أغسطس 2026 — عند الكوميت `37b6b4d` على `main`.
> **تصحيح مهم:** خطة التسليم الأصلية صنّفت لوحة المدير وفريق العمل «Not started» — **وهما مبنيان ومدفوعان**؛ الجدول أدناه هو الحالة المتحققة من المستودع.

---

## 1. الحالة الفعلية (متحقَّقة من Git والكود)

| المؤشر | القيمة |
|---|---|
| الفرع | `main` متزامن مع `origin/main` · شجرة العمل نظيفة |
| الكوميتات | 25 · الأحدث `37b6b4d feat: add merchant team and permissions screen` |
| حجم الكود | 127 ملفاً في `src/` · ~17,400 سطر |
| المكونات المشتركة | 34 مكوناً + أدوات (`cx`, `field-utils`, `icons`) |
| الشاشات في المبدّل التطويري | **24** |
| التبعيات | `react` + `react-dom` فقط — **بلا أي مكتبة UI** |
| اللينت والبناء | نظيفان عند هذا الكوميت |

### ما بُني — لوحة التاجر (13 شاشة)
مصادقة (دخول/تسجيل) · تفعيل OTP واستعادة كلمة المرور · معالج إعداد المتجر · نظرة عامة · المنتجات · نموذج منتج بالمتغيرات · المخزون · الطلبات · تفاصيل الطلب · الخصومات · المظهر والقوالب (D6 محسوم) · **فريق العمل** · إعدادات المتجر والنشر.

### ما بُني — واجهة الزبون (5 شاشات)
التصفح · صفحة المنتج · السلة · إتمام الشراء · تتبع الطلب — فوق قشرة ستورفرونت وثيم تاجر وسلة مشتركة.

### ما بُني — لوحة مدير المنصة (6 شاشات)
الرئيسية · طلبات الاعتماد + شاشة المراجعة · المتاجر + درج المعاينة · فحص المنتجات · باقات الاشتراك · سجل التدقيق (قراءة فقط).

### ما لم يُبنَ
- **Routing حقيقي** (المبدّل التطويري في `App.tsx` ما زال وسيلة التنقل)
- **أي ربط بالباكند** أو مصادقة فعلية أو استمرارية بيانات
- المسارات المرهونة بقرارات معلّقة (انظر §3)
- قسم الملف الشخصي والأمان للتاجر · إدارة التصنيفات · إشعارات

---

## 2. المعمار المعتمد (لا يُغيَّر دون سبب)

Vite · React 19 · TypeScript · **CSS خام بـ Tokens دلالية** · بلا مكتبة UI · بلا Router بعد · بلا استمرارية (لا Storage ولا Cookies) · بيانات تجريبية محلية مكتوبة الأنواع · حالة معاينة مشتركة بـ `useSyncExternalStore` (ثيم الستورفرونت + السلة).

**أساسات لا تُكرَّر:** أنماط RTL العامة · الـ Tokens · بدائيات الواجهة · `AppShell`/`Sidebar`/`Topbar`/`Tabs`/`Table`/`DataTable`/`Pagination`/`Stepper`/`Drawer` · ملاحة التاجر المشتركة · ملاحة المدير المشتركة · قشرة الستورفرونت · قواميس الحالات في `src/types/status.ts` · أساسيات الثيم في `src/types/store-theme.ts`.

**التشغيل:** `npm run dev` → `http://localhost:5173/`

---

## 3. القرارات المعلّقة

التفصيل الكامل بالخيارات والأثر في **`docs/project/requirements-gap-resolution.md`**.

- **حاجبة:** D1 الشحن · D3 وثائق التوثيق · D5 تعدد المتاجر (تصديق) · D2 الدفع بالبطاقة
- **متوسطة:** D9 الخصم · D10/G2 الصلاحيات · D4/M2 الاشتراك عند التاجر · D7/M3 الإشعارات · D8 انتهاء الحجز
- **منخفضة:** G1 · G6 · G4 · G9 · M5 · M8 · M9 · G7 · G8 · M4 · M10
- **محسوم:** ✅ D6 حرية ألوان المتجر (16 أغسطس 2026)

**قاعدة ملزمة:** لا يُنفَّذ أي سلوك معلّق كأنه نهائي — يُعرض Placeholder موسوماً أو صف «بانتظار قرار المنتج».

---

## 4. التسلسل القادم

| المرحلة | المحتوى | الحالة |
|---|---|---|
| 0 | تدقيق المشروع | ✅ منجزة |
| 1 | تثبيت عمل الزبون | ✅ منجزة (كل شيء في `main`) |
| 2 | لوحة مدير المنصة | ✅ منجزة (6 شاشات) |
| 3 | باقي شاشات التاجر | ✅ منجزة عملياً (بقي الملف الشخصي والأمان) |
| **4** | **حل فجوات المتطلبات** (توثيق فقط) | 🔵 **جارية** — الوثيقة أُنشئت وتنتظر قرارات مالك المنتج |
| **5** | **Routing وبنية التطبيق** | ⏭️ التالية — تحتاج تبعية Router (تُشرح قبل التثبيت) |
| 6 | تدقيق وربط الـ API (Laravel) | بانتظار تفويض صريح |
| 7 | جهوزية الإنتاج (QA، أمان، نشر) | لاحقاً |

### حدود المرحلة 5 (Routing)
مجموعات مسارات: عام/زبون · تاجر · مدير · **حُرّاس المسارات تُؤجَّل** حتى وجود عقود مصادقة فعلية · تبقى البيانات التجريبية ومحوّلاتها · **لا يُحذف نظام المعاينة** إلا بعد التحقق من ربط الـ API.

### حدود المرحلة 6 (الباكند)
لا تُنفَّذ إلا بتفويض صريح · لا أوامر قواعد بيانات مدمرة · تنتج `docs/api/backend-frontend-gap-report.md` · لا تُنشأ نقاط نهاية ناقصة قبل مراجعة التدقيق والقرارات.

---

## 5. قواعد العمل لكل شاشة جديدة

1. اقرأ المتطلبات ووثائق UX/التصميم ذات الصلة، واستخرج الحقول والأفعال **الموثقة حصراً**.
2. افحص حالة Git — **وتوقّف إن وُجدت تغييرات غير مكوّمتة لا تتعلق بمهمتك**.
3. أنشئ فرعاً مركّزاً من الفرع الصحيح (عادةً `main`).
4. نفّذ ببيانات تجريبية محلية مكتوبة الأنواع.
5. أعد استخدام المكونات والـ Tokens — **لا تكرّر** ثيماً أو ملاحة أو سلة أو قاموس حالات.
6. تحقّق: لينت · بناء إنتاجي · خادم تطوير · كونسول نظيف · مكتبي · **هاتف 390px حقيقي** · RTL والمحتوى مختلط الاتجاه · لوحة المفاتيح.
7. أبلِغ: الملفات، السلوك، الافتراضات، المؤجلات، نتائج التحقق، حالة Git.
8. **لا staging ولا commit ولا push ولا PR إلا بطلب صريح.**

### أعراف الالتزام المتبعة (وأثبتت نفسها)
- كل شاشة فيها **أداة معاينة تطويرية موسومة** (fieldset بحد متقطع) لحالات: عادية/تحميل/فارغ/خطأ.
- كل فعل محلي موسوم صراحةً «معاينة محلية — لا حفظ فعلياً».
- الأفعال المدمرة بحوار يشرح الأثر + سبب إلزامي حيث تنص المتطلبات.
- ألوان الحالات محجوزة للدلالة، ولا أزرار «خضراء» زخرفية.
- لا بيانات تاجر داخلية في واجهة الزبون (لا SKU ولا مخزون ولا محجوز).
- التحقق البصري والوظيفي بقيادة المتصفح فعلياً (CDP) لا بالتخمين.

---

## 6. سجل التصحيحات على الخطة الأصلية

| البند في الخطة الأصلية | التصحيح |
|---|---|
| «Platform administrator UI: Not started» | مبنية ومدفوعة — `72b8d89` (6 شاشات) |
| «Merchant team management: Not started» | مبنية ومدفوعة — `37b6b4d` |
| «Merchant core: Nearly complete» | مكتملة (13 شاشة) |
| «المرحلة 2 التالية: لوحة المدير» | منجزة — التالي فعلياً المرحلة 4 ثم 5 |
| قائمة المكونات | أُضيفت `Drawer` و`OtpInput` و`Stepper` و`Menu` ونوع `date`، والمجموع 35 |
| «افتراض A1: تاجر واحد = متجر واحد» | ❌ منقوض بمخطط الباكند — بُني مبدّل المتاجر وسياق «المتجر النشط» لكل شاشات التاجر (D5 محسوم، 17 أغسطس 2026) |
