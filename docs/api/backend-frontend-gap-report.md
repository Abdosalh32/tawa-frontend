# تقرير فجوة الباكند والواجهة — منصة توا

> **نطاق التدقيق:** قراءة فقط لنسخة الباكند `tawa-backend-main` (Laravel 13 · Sanctum · spatie/laravel-permission · بنية Domains) بتاريخ 17 أغسطس 2026.
> **لم يُعدَّل أي ملف باكند، ولم تُنفَّذ أي أوامر artisan أو قواعد بيانات.**
> **قرار مالك المنتج (17 أغسطس 2026):** ✅ **الباكند هو المرجع عند أي تعارض**، والمكوّنات الناقصة خلفياً تُبنى لاحقاً.
> **حالة الواجهة المرجعية:** الكوميت `37b6b4d` على `main` — 24 شاشة ببيانات تجريبية محلية.

---

## 1. الخلاصة التنفيذية

| المؤشر | القيمة |
|---|---|
| نطاقات الباكند المبنية | 7: MerchantAuth · Store · Theme · Product · Discount · Order · Team |
| المسارات المتاحة (كلها `v1`) | ~55 مساراً — **كلها للتاجر** خلف `auth:sanctum` (+ `/health`) |
| الجداول | 21 هجرة · **بلا أي جدول للمدير أو الاعتماد أو الاشتراكات أو التدقيق** |
| مسارات الزبون العامة | **صفر** — لا تصفح ولا سلة ولا checkout ولا تتبع |
| تغطية شاشات الواجهة | **13 من 24** لها مسارات جاهزة · 5 بلا باكند · 6 جزئية |
| تعارضات تحتاج تعديل الواجهة | **6** (كلها تُحسم لصالح الباكند) |
| أسئلة منتج باقية | **2 فقط**: D1 الشحن · D2 الدفع |

---

## 2. ما حسمه الباكند من قرارات معلّقة

| القرار | كان معلّقاً | الجواب من الباكند | الدليل |
|---|---|---|---|
| **D5** تعدد المتاجر | متجر واحد؟ | ✅ **متاجر متعددة لكل تاجر** | `stores.merchant_id` بلا فرادة · `GET /v1/merchant/stores` (قائمة) · كل المسارات `/stores/{id}/...` |
| **D9** الخصم | تلقائي أم بكود؟ | ✅ **بكود إلزامي** + نوعان: `fixed` و**`percentage`** + سقف خصم + حد استخدام | `discounts` · `DiscountRequest` · `POST /discounts/validate` |
| **D10/G2** الصلاحيات | مفاتيح أم أدوار؟ | ✅ **4 أدوار جاهزة + 20 صلاحية بحبيبية الإجراء** | `RolePermissionSeeder` · `InviteStaffRequest` · `GET /v1/merchant/roles` |
| **D8** انتهاء الحجز | المدة والسلوك؟ | ✅ **15 دقيقة** (`durationMinutes = 15`) + `release` صريح + `is_released` | `ReserveStockDTO` · `InventoryService` |
| **G4** حد المخزون | لكل منتج أم عام؟ | ✅ **لكل منتج ولكل متغير**، افتراضي **5** | `products.low_stock_threshold` · `product_variants.low_stock_threshold` |
| **M5** التصنيفات | مركزية أم محلية؟ | ✅ **لكل متجر** + شجرة `parent_id` | `categories.store_id` |
| **D6** الألوان | (محسوم مسبقاً) | ✅ مؤكَّد **+ عمودان إضافيان**: `header_bg` و`footer_bg` | `CustomizeThemeRequest` · `themes.default_colors` |
| **1.3.4** ترتيب الأقسام | مؤجل | ✅ **مدعوم**: `sections` json بـ`id/type/title/visible/settings` | `store_themes.sections` · `ThemeSeeder` |
| **OTP** | الصلاحية والمحاولات؟ | ✅ **10 دقائق** صلاحية · `attempts` مسجّلة · `code(10)` · 3 أنواع | `OtpService` · `merchant_otps` |
| **النطاق المخصص** | مؤجل (Future) | ✅ **مخطط**: `type` = subdomain\|custom + `verifyDomain` | `store_domains` |
| **G9** الإنجليزية | ضمن MVP؟ | ✅ **مدعومة**: `language in:ar,en` على المتجر | `CreateStoreRequest` · `PUT /stores/{id}/language` |

---

## 3. التعارضات الستة — الباكند يفوز (قرار مؤكَّد)

### ت١ · حالات الطلب: **ست لا خمس**
- **الباكند:** `pending → confirmed → processing → ready_for_delivery → delivered` + `cancelled`
- **الواجهة الآن:** `confirmed → preparing → ready → delivered` + `cancelled` (**بلا `pending`**)
- **الأثر:** `src/types/status.ts` (قاموس `ORDER_STATUS`) · شاشة الطلبات (تبويبات وعدّادات) · تفاصيل الطلب (Stepper ومنطق «نقل إلى») · تتبع الزبون (الخط الزمني)
- **حجم التعديل:** صغير-متوسط — إضافة حالة وإعادة تسمية اثنتين

### ت٢ · نموذج حالة ثالث: `fulfillment_status`
- **الباكند:** `unfulfilled | fulfilled` مستقلاً عن حالة الطلب والدفع
- **الواجهة الآن:** غير موجود (تجنّبته لغيابه عن المتطلبات)
- **الأثر:** عمود/شارة إضافية في الطلبات وتفاصيلها + فلتر ثالث (يطابق «حالات منفصلة: الطلب/الدفع/التوصيل» في 1.5.2)
- **حجم التعديل:** صغير

### ت٣ · الخصم: **كود + نسبة مئوية**
- **الباكند:** `code` إلزامي · `type` fixed\|percentage · `max_discount_amount` · `usage_limit`/`used_count` · مسار تحقق من الكوبون
- **الواجهة الآن:** مبلغ ثابت بلا كود؛ والسلة والـ checkout بصف «بانتظار D9»
- **الأثر:** شاشة الخصومات (حقل كود + نوع + سقف + حد استخدام) · **حقل كود في السلة** + استدعاء التحقق · سطر الخصم الفعلي · `compare_at_price` يفتح السعر المشطوب
- **حجم التعديل:** **متوسط** — أكبر تعديل واجهة في القائمة

### ت٤ · الاسترجاع موجود في الـ schema
- **الباكند:** `payment_status` يشمل `refunded` (بلا مسار مخصص — يُضبط عبر `PUT /orders/{id}/status`)
- **الواجهة الآن:** شارتان فقط (كاش/مدفوع)
- **الأثر:** إضافة شارة «مُسترجع» + قيمة `unpaid` الحقيقية (بدل تمثيلي «كاش عند الاستلام»)

### ت٥ · حالة المتجر vs التاجر
- **الباكند:** المتجر `draft|active|maintenance` — **ولا تعليق/حظر على المتجر**؛ التعليق على التاجر: `merchants.status = suspended`
- **الواجهة الآن:** شاشة المتاجر الإدارية **تعلّق وتحظر المتجر**
- **الأثر:** إعادة توجيه فعل التعليق إلى **التاجر** لا المتجر، وحالة «محظور» غير موجودة خلفياً (تحتاج قراراً أو عموداً جديداً)

### ت٦ · حقول منتج إضافية غير مستخدمة في الواجهة
`slug` · `short_description` · `meta_title/meta_description` (للمتجر) · `compare_at_price` · `images` json · نوع الخاصية `select|color|radio` + `color_code` للقيم اللونية → **محاور المتغيرات حرة الأسماء** (لا مقاس/لون فقط كما بنيت)

---

## 4. خريطة الشاشات ← المسارات

### ✅ جاهزة للربط (13 شاشة)

| الشاشة | المسارات |
|---|---|
| مصادقة (دخول/تسجيل) | `POST /v1/merchant/auth/register` · `/login` · `/logout` |
| تفعيل OTP | `POST /auth/verify-otp` · `/resend-otp` |
| استعادة كلمة المرور | `POST /auth/forgot-password` · `/reset-password` |
| الملف الشخصي (غير مبني بعد) | `GET|PUT /auth/profile` · `POST /auth/change-password` |
| معالج إعداد المتجر | `POST /stores` (name · subdomain · language) |
| إعدادات المتجر والنشر | `PUT /stores/{id}` · `publish` · `unpublish` · `updateSubdomain` · `PUT /language` |
| المظهر والقوالب | `GET /themes/library` · `POST /themes` (تثبيت) · `PUT /customize` · `GET /preview` · `POST /publish` |
| المنتجات | `GET|POST /products` · `PUT /products/{id}` · `archive`/`unarchive` |
| نموذج المنتج والمتغيرات | `POST|PUT|DELETE /products/{id}/variants` |
| المخزون | `POST /products/{id}/stock` · `GET /inventory/alerts` · `reserve`/`release` |
| الطلبات | `GET /orders` · `GET /orders/{id}` |
| تفاصيل الطلب | `PUT /orders/{id}/status` · `POST /orders/{id}/cancel` |
| فريق العمل | `GET /roles` · `GET /team` · `POST /team/invite` · `PUT /team/{id}/role` · `POST /team/{id}/suspend` |

### ⚠️ جزئية

| الشاشة | الناقص |
|---|---|
| الخصومات | لا مسار **تعديل** (`PUT`) — فقط إنشاء/حذف/تحقق |
| نظرة عامة (التاجر) | لا مسار إحصائيات — تُجمَّع من مسارات قائمة أو تحتاج endpoint |
| التصنيفات | المسارات موجودة (`/categories`) **ولا شاشة في الواجهة** |
| المتغيرات | لا مسار لإدارة `product_attributes` مستقلاً (تُنشأ ضمن المنتج) |

### 🔴 بلا أي باكند (11 شاشة/مسار)

| المجموعة | التفصيل |
|---|---|
| **واجهة الزبون كاملة (5 شاشات)** | لا مسارات عامة: تصفح · صفحة منتج · سلة · checkout · تتبع. **ملاحظة حرجة:** `POST /orders` موجود **تحت التاجر خلف Sanctum** — إنشاء الطلب من الزبون (2.2.6) يحتاج مساراً عاماً |
| **لوحة المدير كاملة (6 شاشات)** | لا `admins` ولا اعتماد ولا `merchant_documents` (D3) ولا `plans`/`subscriptions` (D4/G6) ولا `audit_logs` (م.1.3.13) ولا حظر منتج |
| الشحن | `orders.shipping_fee` عمود فقط — **بلا مصدر ولا جدول مناطق** (D1) |
| الدفع | لا `payments`/`transactions` ولا بوابة (D2) |
| الإشعارات والسياسات | لا جدولين ولا مسارات (D7 · G1) |

---

## 5. عقود مهمة للربط (مستخرجة من قواعد التحقق)

**التسجيل:** `name` · `email` (فريد) · `phone` (فريد، ≤20) · `password` (≥8) — **مطابق للواجهة تماماً**
**إنشاء المتجر:** `name` · `subdomain` · `language in:ar,en` — **مطابق للمعالج**
**إنشاء الطلب:** `customer_name` · `customer_phone` · `shipping_address.city` + `.address` **(كائن)** · `items[].product_id/variant_id/quantity` · `shipping_fee?` · `discount_code?`
→ **يطابق حقول الـ checkout المبنية** (الاسم/الهاتف/المدينة/العنوان) · **ولا حقل «مكان الاستلام» ولا «ملاحظات»** في العقد
**دعوة موظف:** `name` · `email` · `phone` **(الهاتف إلزامي)** · `role_name` من الأربعة
→ الواجهة تطلب البريد فقط → **تحتاج حقلي الاسم والهاتف + منتقي الدور**
**تخصيص الثيم:** `colors{primary, secondary, header_bg, footer_bg, text_color, background}` (الخلفيتان ≤100 حرف وتقبلان `rgba()`، والبقية ≤50) + `sections[]` بعناصر `{id, type, title, visible, settings}`
→ ✅ **طُبّق (17 أغسطس 2026):** الأدوار الستة بأسمائها الحرفية · `surface` **غير موجود في العقد** فلون البطاقات يُشتق محلياً من `background` · وترتيب `sections` هو ترتيب العرض (1.3.4)
**دورة حياة الثيم:** التثبيت يُنشئ صف `store_themes` **كمسودة** (`is_live=false`) ينسخ `default_colors`/`default_sections`، والنشر يرفع واحداً فقط، ولا يُحذف المنشور
→ ✅ طُبّق: شاشة المظهر تفصل «تثبيت كمسودة» عن «نشر»، وتمنع حذف المنشور
**استجابة المتجر:** `{id, merchant_id, name, logo, language, status, meta_title, meta_description, published_at, domains[], created_at}` — تواريخ ISO 8601

---

## 6. تقدير إعادة العمل في الواجهة (لا يُنفَّذ قبل أمرك)

| # | العمل | السبب | الحجم | الحالة |
|---|---|---|---|---|
| 1 | قاموس حالات الطلب + الحالة الثالثة | ت١ · ت٢ | صغير-متوسط | ✅ منجز — 17 أغسطس 2026 |
| 2 | الخصومات بكود ونسبة + حقل كود في السلة + السعر المشطوب | ت٣ | **متوسط** | ✅ منجز — 17 أغسطس 2026 |
| 3 | مبدّل متاجر + «سياق متجر نشط» في كل شاشات التاجر | D5 محسوم بالتعدد | **متوسط-كبير** | ✅ منجز — 17 أغسطس 2026 |
| 4 | الصلاحيات: أدوار جاهزة + 20 صلاحية بحبيبية الإجراء | D10 | متوسط | ✅ منجز مع البند 1 |
| 5 | دعوة الموظف: + الاسم والهاتف والدور | العقد | صغير | ✅ منجز مع البند 1 |
| 6 | ثيم: + خلفيتا الهيدر والفوتر · وترتيب الأقسام (1.3.4) · ودورة مسودة/نشر | العقد | صغير + متوسط | ✅ منجز — 17 أغسطس 2026 |
| 7 | شاشة التصنيفات (مسارات جاهزة بلا شاشة) | فجوة | متوسط | ⏳ متبقٍ |
| 8 | شارة «مُسترجع» و`unpaid` الحقيقية | ت٤ | صغير | ✅ منجز مع البند 1 |
| 9 | إعادة توجيه التعليق للتاجر لا المتجر | ت٥ | صغير | ✅ منجز مع البند 1 |

**المتبقي:** (7) شاشة التصنيفات فقط.

---

## 7. الأسئلة الباقية لمالك المنتج

1. **D1 الشحن:** `orders.shipping_fee` يُرسل مع إنشاء الطلب — **من يحسبه؟** (التاجر يدوياً لكل طلب؟ إعداد ثابت؟ جدول مناطق يُبنى لاحقاً؟)
2. **D2 الدفع:** `payment_status` جاهز (unpaid/paid/refunded) بلا بوابة — **متى وأي بوابة؟** وهل الإطلاق بالكاش فقط؟
3. **حالة «محظور» للمتجر:** غير موجودة خلفياً — تُضاف أم يكفي تعليق التاجر؟
4. **أولوية الباكند القادم:** مسارات الزبون العامة (تشغّل 5 شاشات) أم لوحة المدير (تشغّل 6)؟ **توصيتنا: الزبون أولاً** لأنه يكمل حلقة البيع.

---

## 8. ما لم يُفحص (لأمانة التقرير)

- منطق الخدمات (Services) داخلياً بالتفصيل — قرأت العقود والثوابت الحرجة فقط
- الاختبارات إن وُجدت · `.env.example` · إعدادات CORS وSanctum (تلزم قبل أول ربط فعلي)
- سلوك الأخطاء الموحد (شكل رسائل الخطأ) — **ضروري لتطبيع الأخطاء في الواجهة، ويُفحص عند بدء الربط**
